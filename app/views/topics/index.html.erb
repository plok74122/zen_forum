<h1>Topic List</h1>

<% @topics.each do |t| %>
  <table>
   <tr>
	<td><%=	t.title %></td>
	<td><%=	t.user.email %></td>
	<td><%=	t.created_at %></td>
	<td><%=	t.updated_at %></td>

   </tr>
  </table>
<% end %>

<%= paginate @topics %>